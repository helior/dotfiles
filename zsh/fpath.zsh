#add each topic folder to fpath so that they can add functions and completion scripts
# for topic_functions in `find $ZSH/*/functions -type d -not -empty`; do
#   fpath=($topic_functions $fpath)
# done
